const assert = require ('chai').assert;
const editQuery = require ('../rpgplus/src/app/views/diceRollingView');


  it('valor', function(){
    assert.typeOf(value,'integer')
  });
  
  it('soma', function(){
    assert.typeOf(sum,'integer')
  });